---
'@kernhq/module-chat': patch
---

The chat widget's "Rows" setting shows its label again instead of a raw message key; it now reads
the shared `common.setting_rows` string like every other widget.
